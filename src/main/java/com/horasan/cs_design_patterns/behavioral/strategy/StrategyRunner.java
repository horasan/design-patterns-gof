package com.horasan.cs_design_patterns.behavioral.strategy;

public class StrategyRunner {
	// enables the exact behaviour of a system to be selected
	// either at run-time (dynamic) or compile-time (static).
}
